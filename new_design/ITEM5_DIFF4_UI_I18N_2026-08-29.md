# Pacote Pré-Privacy — ITEM 5, DIFF 4: UI + i18n (proposta) — último do pacote

**Data:** 2026-08-29
**Status:** diff completo para revisão — **aguardando OK do Bob. Nada aplicado.**
**Superfície viva confirmada:** o Underground v2 renderiza `components/underground/UndergroundFeed.jsx` dentro da `pages/v2/CommunityPage.jsx:211` (skin em `styles/v2-pages/underground.css`); namespace i18n real: `community.underground.*`. O modal comum é `components/common/ConfirmModal.jsx` (Modal + form-button, com skin v2 via `community-panels.css`) — o ReportModal segue exatamente esse padrão.

---

## 0. Decisões e descobertas (para o OK cobrir)

1. **Lacuna coberta aqui — unlock no front:** o desenho §2.1 manda rodar `ensureUserKeys()` ANTES do unlock; nenhum diff anterior tocou o `SpaceLock.jsx`. Sem isso, usuário sem keypair toma `409 KEYPAIR_REQUIRED` para sempre. Entra neste diff (§1).
2. **Realtime precisa decifrar:** os broadcasts `new-post`/`post-edited` chegam com `encrypted_content`/`nonce` (snake) e, pós-E2E, sem `content` — sem decifra no handler, posts em tempo real apareceriam vazios. O hook passa a decifrar via `cryptoService.decryptUndergroundPost` (§2).
3. **i18n: apêndice, não inserção alfabética** — o bloco `community.underground` tem ordem lógica (não alfabética, ao contrário do `v2.unsafe-zone`); as 14 chaves novas entram como grupo coeso ao FINAL do bloco, mesmo método cirúrgico (byte-estável, +14 linhas/arquivo).
4. **`reportPost` ganha `err.status`** (uma linha no serviço, complemento do DIFF 3) — necessário para a UI distinguir 429 (sem `code` no body do rate limiter).
5. **Botão de report só em posts alheios** (denunciar o próprio post não faz sentido); ícone bandeira, classe `chat-msg-action` — o mesmo chrome discreto do delete.
6. **Composer bloqueado fora de `ready`:** textarea+botão desabilitados em `pending`/`error`; o `E2E_NO_ROOM_KEY` fica como defesa em profundidade (traduzido via `errorCode`).

---

## 1. `site/src/components/community/SpaceLock.jsx`

```diff
 import { setPendingAction, getPendingAction, clearPendingAction } from '@utils/pendingAction.js';
 import { logger } from '@utils';
+import * as cryptoService from '@/services/crypto';
@@ handleUnlock @@
   const handleUnlock = async () => {
     setLoading(true);
     setError(null);
 
     try {
+      // UNDERGROUND E2E (design §2.1): register a keypair BEFORE unlocking —
+      // the backend rejects keyless unlocks with 409 KEYPAIR_REQUIRED.
+      if (space === 'underground') {
+        try {
+          await cryptoService.ensureUserKeys();
+        } catch (err) {
+          logger.warn('[SpaceLock] ensureUserKeys failed:', err.message);
+        }
+      }
+
       const response = await fetch(`${config.apiUrl}/api/spaces/${space}/unlock`, {
@@ tratamento de erro @@
         if (response.status === 402 || data.code === 'INSUFFICIENT_CREDITS') {
           setPendingAction({ type: 'space:unlock', space });
           setShowBuyCredits(true);
           setLoading(false);
           return;
         }
+        if (response.status === 409 && data.code === 'KEYPAIR_REQUIRED') {
+          // ensureUserKeys falhou em registrar (rede/API) — erro traduzido
+          throw new Error(t('community.underground.keypairError'));
+        }
         throw new Error(data.error || 'Failed to unlock space');
```

## 2. `site/src/hooks/useUnderground.js`

```diff
 import { undergroundService } from '../services/api/underground.js';
+import * as cryptoService from '@/services/crypto';
@@ estado @@
   const [settingNickname, setSettingNickname] = useState(false);
+  const [roomStatus, setRoomStatus] = useState(null); // 'ready' | 'pending' | 'error' | null
+  const [errorCode, setErrorCode] = useState(null);
@@ broadcast new-post @@
           .on('broadcast', { event: 'new-post' }, ({ payload }) => {
+            // E2E: realtime payloads carry ciphertext only — decrypt here
+            const decrypted = cryptoService.decryptUndergroundPost(
+              payload.encrypted_content,
+              payload.nonce,
+            );
             const newPost = {
               ...payload,
+              encryptedContent: payload.encrypted_content,
+              content: decrypted,
+              decryptionFailed: !decrypted,
               myReactions: [],
             };
@@ broadcast post-edited @@
           .on('broadcast', { event: 'post-edited' }, ({ payload }) => {
+            const decrypted = cryptoService.decryptUndergroundPost(
+              payload.encrypted_content,
+              payload.nonce,
+            );
             setPosts((prev) =>
               prev.map((p) =>
                 p.id === payload.id
                   ? {
                       ...p,
-                      content: payload.content,
+                      content: decrypted,
+                      decryptionFailed: !decrypted,
                       encryptedContent: payload.encrypted_content,
@@ loadPosts @@
       setNeedsNickname(false);
       setMyNickname(result.myNickname || null);
+      setRoomStatus(result.roomStatus || null);
       setPosts(result.posts || []);
@@ createPost catch (idem editPost) @@
       } catch (err) {
         setError(err.message);
+        setErrorCode(err.code || null);
         throw err;
@@ (setError(null) em cada início de ação ganha o par setErrorCode(null)) @@
@@ novo, antes do return @@
+  // Report a post (design §2.8) — the modal owns the lifecycle
+  const reportPost = useCallback(
+    (post, reason) => undergroundService.reportPost(post, reason),
+    []
+  );
@@ return @@
+    roomStatus,
+    errorCode,
+    reportPost,
```

## 3. `site/src/components/underground/UndergroundFeed.jsx`

### 3.1 Ícone + banner de estado da sala + composer bloqueado

```diff
+  Flag: () => (
+    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
+      <path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z" />
+      <line x1="4" y1="22" x2="4" y2="15" />
+    </svg>
+  ),
@@ no corpo do UndergroundFeed, após a identidade @@
+      {/* Room key state (mandatory E2E) — honest pending message (design §2.3) */}
+      {roomStatus === 'pending' && (
+        <div className="underground-room-status">
+          {t('community.underground.roomPending')}
+        </div>
+      )}
+      {roomStatus === 'error' && (
+        <div className="underground-error">{t('community.underground.roomError')}</div>
+      )}
@@ composer @@
-          disabled={posting}
+          disabled={posting || roomStatus !== 'ready'}
           (textarea e botão de submit; o handleSubmit ganha o guard `if (roomStatus !== 'ready') return;`)
@@ error display @@
-      {error && <div className="underground-error">{error}</div>}
+      {error && (
+        <div className="underground-error">
+          {errorCode === 'E2E_NO_ROOM_KEY' ? t('community.underground.noKeyError') : error}
+        </div>
+      )}
```

### 3.2 Botão de report (posts alheios, chrome discreto do padrão delete)

```diff
@@ action bar do UndergroundPost @@
           <button className="chat-msg-action" onClick={handleCopy} ...>
             <Icons.Copy />
           </button>
+          {!isOwn && (
+            <button
+              className="chat-msg-action"
+              onClick={() => onReport?.(post)}
+              title={t('community.underground.report')}
+              aria-label={t('community.underground.report')}
+            >
+              <Icons.Flag />
+            </button>
+          )}
           {isOwn && ( ... delete ... )}
```

(prop `onReport` roteada pelo Feed: `onReport={handleReportRequest}` → `setReportTarget(post)`.)

### 3.3 ReportModal (novo componente no mesmo arquivo, padrão do ConfirmModal/Modal)

```jsx
function ReportModal({ post, onClose, onSubmit, t }) {
  const [reason, setReason] = useState('');
  const [state, setState] = useState('idle'); // idle | sending | success | stale | ratelimited | error

  const handleSend = async () => {
    const trimmed = reason.trim();
    if (!trimmed || state === 'sending') return;
    setState('sending');
    try {
      await onSubmit(post, trimmed);
      setState('success');
    } catch (err) {
      if (err.code === 'REPORT_STALE') setState('stale');       // pós-retry do serviço
      else if (err.status === 429) setState('ratelimited');
      else setState('error');
    }
  };

  return (
    <Modal isOpen={!!post} onClose={onClose} title={t('community.underground.reportTitle')} maxWidth="440px">
      {state === 'success' ? (
        <>
          <p className="text-white-70 mb-6 leading-normal text-sm">
            {t('community.underground.reportSuccess')}
          </p>
          <div className="flex gap-3 justify-end">
            <button className="form-button flex-1" onClick={onClose}>
              {t('community.dm.cancel', 'Close')}
            </button>
          </div>
        </>
      ) : (
        <>
          {/* Aviso explícito e destacado (design §2.8 / decisão do Bob) */}
          <div className="underground-report-warning">
            {t('community.underground.reportWarning')}
          </div>
          <textarea
            className="underground-input__textarea"
            value={reason}
            onChange={(e) => setReason(e.target.value.slice(0, 500))}
            placeholder={t('community.underground.reportReasonPlaceholder')}
            maxLength={500}
            rows={3}
            disabled={state === 'sending'}
            autoFocus
          />
          <div className="underground-input__charcount">{reason.length}/500</div>
          {state === 'stale' && (
            <div className="underground-error">{t('community.underground.reportStale')}</div>
          )}
          {state === 'ratelimited' && (
            <div className="underground-error">{t('community.underground.reportRateLimited')}</div>
          )}
          {state === 'error' && (
            <div className="underground-error">{t('community.underground.reportError')}</div>
          )}
          <div className="flex gap-3 justify-end">
            <button className="form-button form-button--cancel flex-1" onClick={onClose}
              disabled={state === 'sending'}>
              {t('community.dm.cancel')}
            </button>
            <button className="form-button form-button--danger flex-1" onClick={handleSend}
              disabled={!reason.trim() || state === 'sending'}>
              {state === 'sending'
                ? t('community.underground.reportSending')
                : t('community.underground.reportSend')}
            </button>
          </div>
        </>
      )}
    </Modal>
  );
}
```

Renderizado no Feed junto do ConfirmModal de delete:

```jsx
{reportTarget && (
  <ReportModal
    post={reportTarget}
    onClose={() => setReportTarget(null)}
    onSubmit={reportPost}
    t={t}
  />
)}
```

(import adicional: `Modal` de `../common/Modal.jsx`.)

## 4. `site/src/services/api/underground.js` — complemento de 1 linha (429)

```diff
   if (!response.ok) {
     const err = new Error(data.error || 'Failed to submit report');
     err.code = data.code;
+    err.status = response.status;
     throw err;
   }
```

## 5. `site/src/styles/v2-pages/underground.css` — 2 classes novas (tokens v2)

```css
/* Room-key state banner + report warning (item 5, E2E) */
.v2 .underground-room-status{border:1px solid var(--line);background:var(--bgcell);
 padding:12px 16px;margin:0 0 12px;font:400 12.5px/1.6 var(--fu);color:var(--mid)}
.v2 .underground-report-warning{border:1px solid var(--warn);padding:10px 14px;margin:0 0 12px;
 font:500 12px/1.6 var(--fu);color:var(--warn)}
```

(seletores finais casados com o escopo real do arquivo na aplicação.)

## 6. i18n — 14 chaves novas em `community.underground` × 18 línguas

Método: apêndice ao final do bloco (ordem lógica preservada), script com validação byte-a-byte prévia, +14 linhas/arquivo.

**PT-BR (para validação do Bob):**

| Chave | Texto |
|---|---|
| `keypairError` | "Não foi possível preparar suas chaves de criptografia. Recarregue e tente de novo." |
| `roomPending` | "Sala cifrada de ponta a ponta. Sua chave de acesso será entregue pelo navegador de outro membro — volte em breve." |
| `roomError` | "Não foi possível preparar a sala cifrada. Recarregue para tentar de novo." |
| `noKeyError` | "Sua chave da sala ainda não chegou — publicar está bloqueado." |
| `report` | "Denunciar" |
| `reportTitle` | "Denunciar post" |
| `reportWarning` | "Ao denunciar, uma cópia legível deste post será enviada à moderação." |
| `reportReasonPlaceholder` | "Por que você está denunciando este post?" |
| `reportSend` | "Enviar denúncia" |
| `reportSending` | "Enviando…" |
| `reportSuccess` | "Denúncia enviada." |
| `reportStale` | "O post mudou enquanto você denunciava. Revise o novo conteúdo e tente de novo." |
| `reportRateLimited` | "Muitas tentativas. Aguarde um momento e tente de novo." |
| `reportError` | "Falha ao enviar a denúncia. Tente de novo." |

**EN:** keypairError "Could not prepare your encryption keys. Reload and try again." · roomPending "End-to-end encrypted room. Your access key will be delivered by another member's browser — check back soon." · roomError "Could not prepare the encrypted room. Reload to try again." · noKeyError "Your room key has not arrived yet — posting is locked." · report "Report" · reportTitle "Report post" · reportWarning "By reporting, a readable copy of this post will be sent to moderation." · reportReasonPlaceholder "Why are you reporting this post?" · reportSend "Send report" · reportSending "Sending…" · reportSuccess "Report sent." · reportStale "The post changed while you were reporting. Review the new content and try again." · reportRateLimited "Too many requests. Please wait a moment and try again." · reportError "Failed to send the report. Try again."

As 16 demais na aplicação, ancoradas no vocabulário existente de cada arquivo (verbo de denúncia local, termos de criptografia dos DMs, etc.).

## 7. Após o OK

Aplicar (SpaceLock → hook → Feed → serviço 1-linha → CSS → i18n script) → build do site → `git status --short`. Sem commit. **Fecha o pacote** → relatório consolidado `new_design/PACOTE_PRE_PRIVACY_2026-08-29.md`.
